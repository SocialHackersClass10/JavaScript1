'use strict'

/*
    description     exercise with divion remainders
*/

function runExercise09 () {
    console.log("= = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =");
    console.log("JS-1 - week 1 - Exercise 09");
    console.log("Divion remainders");
    console.log("- - - - - - - - - - - - - - - - - - - -");
    let x=7;
    x=x%3;
    console.log("If `x` equals 7, and the only other statement is `x = x % 3`");
    console.log("   then the value of `x` after the calculation will be equal to 1");
    console.log("   because % results in the integer part of the leftover of the division of two numbers");
    console.log("   which in our case is the integer leftover of the division `7/3` , which equals 1");
    console.log(x);
    let y=21;
    y=y%4;
    console.log("If `y` equals 21, and the only other statement is `y = y % 4`");
    console.log("   then the value of `y` after the calculation will be equal to 1");
    console.log("   because % results in the integer part of the leftover of the division of two numbers");
    console.log("   which in our case is the integer leftover of the division `21/4` , which equals 1");
    console.log(y);
    let z=13;
    z=z%2;
    console.log("If `z` equals 13, and the only other statement is `z = z % 2`");
    console.log("   then the value of `z` after the calculation will be equal to 1");
    console.log("   because % results in the integer part of the leftover of the division of two numbers");
    console.log("   which in our case is the integer leftover of the division `13/2` , which equals 1");
    console.log(z);
    console.log("= = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = = =");
    console.log(" ");
};

runExercise09();

