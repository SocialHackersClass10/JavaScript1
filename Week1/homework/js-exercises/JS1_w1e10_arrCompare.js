'use strict'

const arr1 = [123,234,0,1];
const arr2 = [192,168,1,1,8080];

console.log("The length of the arr1 is " + arr1.length + " and the length of the arr2 is " + arr2.length);

if(arr1.length === arr2.length) console.log("arr1 and arr2 has the same length");
else console.log("arr1 and arr2 has not the same length");